{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to myapp (WSL-native)",
      "type": "cppdbg",
      "request": "attach",
      "program": "${workspaceFolder}/main",
      "processId": "${command:pickProcess}",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb"
    }
  ]
}